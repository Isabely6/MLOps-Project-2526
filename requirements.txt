# Kedro 0.18 is required by the ``*DataSet`` catalog entry points used here.
kedro>=0.18,<0.19
pandas>=2.0,<2.3
pyarrow>=12,<19
great-expectations>=0.18,<1.0
pytest>=7,<9
scikit-learn>=1.3,<2.0
xgboost>=2.0,<4.0
lightgbm>=4.0,<5.0
mlflow>=3.14,<4.0
shap>=0.44,<1.0
# Pin shap's compiled transitive deps to versions that ship prebuilt wheels for
# CPython 3.10 (incl. macOS x86_64), so `pip install` never needs CMake/LLVM to
# build llvmlite from source.
numba==0.60.0
llvmlite==0.43.0

# --- Member 4: Deployment & Monitoring ---
# Serving (FastAPI) + drift monitoring (PSI / KS / JS) + plots + test client.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
httpx>=0.27,<1.0            # FastAPI TestClient transport
scipy>=1.10,<2.0           # KS test + Jensen-Shannon (also a sklearn/shap dep)
matplotlib>=3.7,<4.0       # drift distribution plots + MatplotlibWriter outputs
# Evidently (optional, nicer drift_report.html) lives in requirements-monitoring.txt
# because its dependency pins are version-sensitive; the drift code import-guards it
# and renders a self-contained HTML fallback when it is absent.